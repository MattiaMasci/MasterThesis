import torch
from torch import nn
from torchvision.models import vgg16
import numpy as np
import random
import logging, sys
from random import randint, sample

calc = False

for i in range(3):
    for j in range(4):
        if True:
            calc = True
print(calc)